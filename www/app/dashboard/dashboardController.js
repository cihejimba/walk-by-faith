(function() {
    "use strict";

    var module = angular.module("wbf.mobile");

    var dashboardController = function($scope, $rootScope, $stateParams, $ionicNavBarDelegate) {
        // dashboard logic goes here
        // 
    };
    module.controller("DashboardController", ["$scope", "$rootScope", "$stateParams", "$ionicNavBarDelegate", dashboardController]);

}());
