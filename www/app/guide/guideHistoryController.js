(function() {
    "use strict";

    var module = angular.module("wbf.mobile");

    var historyController = function($scope, $state, $translate, ColorService) {

    };
    module.controller("HistoryController", ["$scope", "$state", "$translate", "ColorService", historyController]);

}());
