(function() {
    "use strict";

    var module = angular.module("wbf.mobile");

    var favoritesController = function($scope, $state, $translate, ColorService) {

    };
    module.controller("FavoritesController", ["$scope", "$state", "$translate", "ColorService", favoritesController]);

}());
